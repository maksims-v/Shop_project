import React from 'react';
import type { Meta, StoryObj } from '@storybook/react';
import { SectionCategory } from './SectionCategory';

const meta: Meta<typeof SectionCategory> = {
  title: 'shared/SectionCategory',
  component: SectionCategory,
};

export default meta;

type Story = StoryObj<typeof SectionCategory>;