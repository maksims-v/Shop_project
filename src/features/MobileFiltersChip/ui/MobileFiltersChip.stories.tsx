import React from 'react';
import type { Meta, StoryObj } from '@storybook/react';
import { MobileFiltersChip } from './MobileFiltersChip';

const meta: Meta<typeof MobileFiltersChip> = {
  title: 'shared/MobileFiltersChip',
  component: MobileFiltersChip,
};

export default meta;

type Story = StoryObj<typeof MobileFiltersChip>;